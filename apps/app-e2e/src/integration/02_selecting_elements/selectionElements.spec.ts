describe('Selection elements', async () => {
  before(() => {
    console.log('before');
  });

  beforeEach(() => {
    console.log('before each');
  });

  afterEach(() => {
    console.log('after each');
  });

  after(() => {
    console.log('after');
  });
});
